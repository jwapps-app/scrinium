---
name: Bug report
about: Something behaves differently than it should
labels: bug
---

**What happened, and what did you expect instead?**

**Steps to reproduce**
1.
2.

**Version**
The image tag or commit you are running (`sha-<short>` from the container, or
`git rev-parse --short HEAD`):

**Setup**
- OCR engine: Tesseract in-container / Apple Vision sidecar / capture-time (iOS)
- Where it runs: Docker on Linux / Synology / other
- Client: PWA (which browser) / iOS app

**Logs**
Relevant lines from the `api` or `worker` container. Please skip anything with
tokens, share links, or document contents in it.
